/* 
Lucas Rigo Yoshimura
lucasrigoyoshimura@gmail.com
*/

@cep(5, "';' apos assinatura de metodo",
   "'{' expected", "comp.Compiler.methodDec()")

class A 
  
    func get -> Int ; {  
        return 0;
    }

end

class Program 
  
    func run {
        var A a;

        a = A.new;
        Out.println: a.get();
    }

end
