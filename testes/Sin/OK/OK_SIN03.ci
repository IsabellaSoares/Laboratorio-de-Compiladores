/*
Renan Rossignatti de França
renanrfranca@gmail.com
*/

@nce

/*
@filename   OK-SIN03.ci
    
@comment    Testa expressoes aritmeticas e logicas, menos unario, &&, || e
todos os operadores de comparacao
*/


class Program

    func run {
        var Int i;
        var Boolean b;
        
        i = (-7) + ((2) * (3 + 5));
        i = (-7) + 8;
        b = ((((((!true) || (i <= 0)) || (i < 0)) || (i >= 0)) || (i > 0)) || (i == 7)) 
            || (i != 3);
    }

end


