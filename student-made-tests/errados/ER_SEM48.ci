@annot("check", "illegalClassName")
@cep(3, "Os nomes dos tipos básicos são reservados e não podem ser nomes de classes",
    "Identifier expected", "comp.Comp.classDec()")

class Int { //#
}



class Program 
  
    func run { } 

end
